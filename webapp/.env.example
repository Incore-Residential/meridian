# Optional: override backend URL for the API client. Leave blank to use relative URLs
# (which work both in production behind a single domain and in dev via the Vite proxy).
# VITE_BACKEND_URL=http://localhost:3000

# Optional: override the dev-server proxy target (defaults to http://localhost:3000).
# VITE_BACKEND_PROXY_TARGET=http://localhost:3000
